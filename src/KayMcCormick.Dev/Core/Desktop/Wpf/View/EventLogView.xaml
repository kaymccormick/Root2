<UserControl
  x:Class="KayMcCormick.Lib.Wpf.View.EventLogView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  DataContext="{Binding RelativeSource={RelativeSource Self},Path=ViewModel}"
  xmlns:local="clr-namespace:KayMcCormick.Lib.Wpf.View"
  mc:Ignorable="d"
  d:DesignHeight="450" d:DesignWidth="800">
  <Grid>
    <ListView
      ItemsSource="{Binding EventLogCollectionView}">
      <ListView.InputBindings>
        <KeyBinding Key="Enter" Command="Open" />
        <MouseBinding MouseAction="LeftDoubleClick"
                      Command="Open" />
      </ListView.InputBindings>
      <ListView.View>
        <GridView>
          <GridViewColumn Header="Time Written"
                          DisplayMemberBinding="{Binding TimeWritten}" />
          <GridViewColumn Header="Message"
                          DisplayMemberBinding="{Binding Path=Exception1}" />
        </GridView>
      </ListView.View>
    </ListView>
  </Grid>
</UserControl>