<Window Name="compilationView"
        x:Class="AnalysisControls.CompilationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AnalysisControls"
        xmlns:syntax="clr-namespace:Microsoft.CodeAnalysis.CSharp.Syntax;assembly=Microsoft.CodeAnalysis.CSharp"
        xmlns:codeAnalysis="clr-namespace:Microsoft.CodeAnalysis;assembly=Microsoft.CodeAnalysis"
        xmlns:cSharp="clr-namespace:Microsoft.CodeAnalysis.CSharp;assembly=Microsoft.CodeAnalysis.CSharp"
        mc:Ignorable="d"
        FontSize="20.0"
        Padding="25"
        DataContext="{Binding ElementName=compilationView,Path=ViewModel}"
        Title="CompilationView" Height="450" Width="800">
  <Window.Resources>
    <Style TargetType="GroupBox">
      <Setter Property="ContentTemplate">
        <Setter.Value>
          <DataTemplate DataType="{x:Type syntax:ClassDeclarationSyntax}">
            <TextBlock Text="{Binding Path=Keyword}"></TextBlock>
          </DataTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <local:Converter1 x:Key="Converter1" />
    <local:Converter2 x:Key="Converter2" />
    <CollectionViewSource
      Source="{Binding Path=AnalyseContext.CurrentRoot.Members}"
      x:Key="Compilation" />
    <ObjectDataProvider
      ObjectType="{x:Type codeAnalysis:SyntaxNode}"
      MethodName="ChildNodes" x:Key="ChildNodes" />
    <ObjectDataProvider
      ObjectType="{x:Type codeAnalysis:SyntaxNode}"
      MethodName="ChildTokens" x:Key="ChildTokens" />
    <ObjectDataProvider x:Key="DataProvider"
                        MethodName="ChildNodesAndTokens" />
    <HierarchicalDataTemplate
      DataType="{x:Type codeAnalysis:SyntaxNode}"
      ItemsSource="{Binding  Converter={StaticResource Converter1}}">
      <TextBlock FontStyle="Italic"
                 Text="{Binding Converter={StaticResource Converter1}}" />
    </HierarchicalDataTemplate>
    <HierarchicalDataTemplate
      DataType="{x:Type syntax:NamespaceDeclarationSyntax}"
      ItemsSource="{Binding Path=Members}">
      <TextBlock>
        <Run
          Text="{Binding Path=NamespaceKeyword.Text,Mode=OneWay}" />
        <Run Text="{Binding Name,Mode=OneWay}" />
      </TextBlock>
    </HierarchicalDataTemplate>
    <HierarchicalDataTemplate
      DataType="{x:Type syntax:StatementSyntax}"
      ItemsSource="{Binding Converter={StaticResource Converter1}}">
      <HierarchicalDataTemplate.Resources>
      </HierarchicalDataTemplate.Resources>
      <TextBlock
        Text="{Binding RelativeSource={RelativeSource PreviousData},Converter={StaticResource Converter1}}" />
    </HierarchicalDataTemplate>
    <HierarchicalDataTemplate
      DataType="{x:Type syntax:MethodDeclarationSyntax}"
      ItemsSource="{Binding Path=Body.Statements}">
      <TextBlock><Run Text="{Binding ReturnType,Mode=OneWay}"/> <Run Text="{Binding Identifier.Text,Mode=OneWay}" /></TextBlock>
    </HierarchicalDataTemplate>
    <HierarchicalDataTemplate
      DataType="{x:Type syntax:ClassDeclarationSyntax}"
      ItemsSource="{Binding Path=Members}">
      <StackPanel Orientation="Horizontal">
        <ItemsControl ItemsSource="{Binding Modifiers}">
          <ItemsControl.Template>
            <ControlTemplate TargetType="ItemsControl">
              <ItemsPresenter />
            </ControlTemplate>
          </ItemsControl.Template>
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <WrapPanel />
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding Text,Mode=OneTime}"
                         Margin="0,0,10,0" />
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <TextBlock>
          <Run Text="{Binding Path=Keyword,Mode=OneWay}" />
          <Run Text="{Binding Identifier.Text,Mode=OneWay}" />
        </TextBlock>
      </StackPanel>
    </HierarchicalDataTemplate>
  </Window.Resources>
  <Grid DataContext="{Binding Source={StaticResource Compilation}}">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="3*" />
      <ColumnDefinition Width="5*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <StackPanel Grid.Column="0" Grid.Row="1"
                Orientation="Horizontal" />
    <TreeView Name="tv"  Grid.Column="0" Grid.Row="2"
              ItemsSource="{Binding Source={StaticResource Compilation}}" SelectedItemChanged="tv_SelectedItemChanged">
    </TreeView>

    <GroupBox Content="{Binding ElementName=tv,Path=SelectedItem}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Column="1" Grid.Row="2" >
    </GroupBox>

  </Grid>
</Window>