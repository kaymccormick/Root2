<UserControl x:Class="AnalysisControls.Views.TypesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AnalysisControls"
             xmlns:syntax="clr-namespace:AnalysisControls.Syntax"
             DataContext="{Binding RelativeSource={RelativeSource Self},Path=ViewModel}"
             mc:Ignorable="d" FontSize="20"
             d:DesignHeight="450" d:DesignWidth="800">
  <UserControl.Resources>
    <Style x:Key="S1" TargetType="TextBlock">
      <Style.Triggers>
        <DataTrigger
          Binding="{Binding Path=Type.IsAbstract}"
          Value="True">
          <Setter Property="Background" Value="Gray" />
        </DataTrigger>
      </Style.Triggers>
    </Style>
    <HierarchicalDataTemplate
      DataType="{x:Type syntax:AppTypeInfo}"
      ItemsSource="{Binding SubTypeInfos}">
      <TextBlock Text="{Binding Title}"
                 Style="{StaticResource S1}" />
    </HierarchicalDataTemplate>
  </UserControl.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <TreeView x:Name="tv"
              ItemsSource="{Binding Root.SubTypeInfos}" />
    <StackPanel Grid.Column="1" Grid.Row="0"
                Orientation="Vertical">
      <StackPanel.Resources>
        <DataTemplate
          DataType="{x:Type syntax:ComponentInfo}">
          <TextBlock>
            <Run Text="{Binding Path=PropertyName}" />
            <Run Text=": " />
            <Run Text="{Binding Path=TypeInfo.Title}" />
          </TextBlock>
        </DataTemplate>
      </StackPanel.Resources>
      <ListBox
        ItemsSource="{Binding ElementName=tv,Path=SelectedItem.FactoryMethods}" />
      <ListBox
        ItemsSource="{Binding ElementName=tv,Path=SelectedItem.Components}" />
    </StackPanel>
  </Grid>
</UserControl>