<kmWpf:AppWindow x:Class="ProjInterface.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjInterface"
        xmlns:kmWpf="http://kaymccormick.com/xaml/kmwpf"
        xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock"
        DataContext="{Binding RelativeSource={RelativeSource Self},Path=ViewModel}"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
  <kmWpf:AppWindow.Resources>
    <kmWpf:GetTypeConverter x:Key="GetTypeConverter"/>
    <local:DictionaryConverter x:Key="DictionaryConverter" />
    <DataTemplate x:Key="Template1" >
      <TextBlock Text="{Binding Metadata,Mode=OneWay,Converter={StaticResource DictionaryConverter},ConverterParameter=Title}"></TextBlock>
    </DataTemplate>
  </kmWpf:AppWindow.Resources>
  <kmWpf:AppWindow.CommandBindings>
    <CommandBinding Command="Open" Executed="CommandBinding_OnExecuted"></CommandBinding>
  </kmWpf:AppWindow.CommandBindings>
  <xcad:DockingManager AllowMixedOrientation="True">
    <xcad:LayoutRoot><xcad:LayoutPanel Orientation="Horizontal">
      <xcad:LayoutAnchorablePane DockWidth="400">
        <xcad:LayoutAnchorable>
          <ListBox Name="viewList" ItemsSource="{Binding Path=Views}" ItemTemplate="{StaticResource Template1}">
            <ListBox.InputBindings>
              <KeyBinding Key="Enter" Command="Open" CommandParameter="{Binding ElementName=viewList,Path=SelectedItem}"></KeyBinding>
            </ListBox.InputBindings>
          </ListBox>
        </xcad:LayoutAnchorable>
      </xcad:LayoutAnchorablePane>
      <xcad:LayoutDocumentPaneGroup>
        <xcad:LayoutDocumentPane x:Name="docpane">
          <xcad:LayoutDocument>
            <local:Shell></local:Shell>
          </xcad:LayoutDocument>
        </xcad:LayoutDocumentPane>
      </xcad:LayoutDocumentPaneGroup>
    </xcad:LayoutPanel></xcad:LayoutRoot>
  </xcad:DockingManager>
</kmWpf:AppWindow>
