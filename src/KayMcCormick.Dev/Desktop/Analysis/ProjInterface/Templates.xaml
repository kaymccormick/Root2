<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ProjInterface"
                    xmlns:codeAnalysisApp1="clr-namespace:CodeAnalysisApp1;assembly=CodeAnalysisApp1">
  <DataTemplate DataType="{x:Type codeAnalysisApp1:LogInvocation}" x:Key="{x:Type codeAnalysisApp1:LogInvocation}">
	<Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Background="AntiqueWhite" ShowGridLines="True">
      <Grid.ColumnDefinitions>
        <ColumnDefinition  Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Margin="5" Grid.Column="0"  Text="{Binding Path=MethodDisplayName}"/>
      <TextBlock Margin="5" Grid.Column="1" Text="{Binding Path=Document.FilePath}"/>
      <TextBlock Margin="5" Grid.Column="2" Text="{Binding Path=SourceLocation}"/>
	</Grid>
  </DataTemplate>
</ResourceDictionary>