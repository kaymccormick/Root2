<RibbonWindow x:Class="Client2.Client2Window1"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:local="clr-namespace:Client2"
              xmlns:anal="http://kaymccormick.com/xaml/analysis"
              xmlns:kmWpf="http://kaymccormick.com/xaml/kmwpf"
              xmlns:primitives="clr-namespace:System.Windows.Controls.Ribbon.Primitives;assembly=System.Windows.Controls.Ribbon"
              xmlns:ribbon="clr-namespace:AnalysisControls.RibbonM;assembly=AnalysisControls"
              mc:Ignorable="d"
              DataContext="{Binding RelativeSource={RelativeSource Self}}"
              Title="Client2Window1" Height="450" Width="800">
  <RibbonWindow.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary
          Source="pack://application:,,,/AnalysisControls;component/RibbonModel.xaml" />
        
        <ResourceDictionary

          Source="pack://application:,,,/WpfLib;component/SharedResources.xaml" />
        <ResourceDictionary
          Source="pack://application:,,,/WpfLib;component/ResourcesMisc.xaml" />
        <ResourceDictionary
          Source="pack://application:,,,/AnalysisControls;component/Templates.xaml" />
        <ResourceDictionary Source="RibbonResources.xaml" />
        <ResourceDictionary />
      </ResourceDictionary.MergedDictionaries>
      <local:TabItemTemplateSelector x:Key="TabItemTemplateSelector"></local:TabItemTemplateSelector>
    </ResourceDictionary>
  </RibbonWindow.Resources>
  <Grid VerticalAlignment="Stretch">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Ribbon ContextualTabGroupsSource="{Binding ContextualTabGroups}"  ItemTemplateSelector="{StaticResource TabItemTemplateSelector}"  x:Name="_ribbon" DataContext="{Binding ViewModel.Ribbon}" ItemsSource="{Binding RibbonItems}">
      <Ribbon.ContextualTabGroupHeaderTemplate>
        <DataTemplate>
          <TextBlock Text="{Binding Header}"/>
        </DataTemplate>
      </Ribbon.ContextualTabGroupHeaderTemplate>
      <Ribbon.ContextualTabGroupStyle>
        <Style TargetType="{x:Type RibbonContextualTabGroup}">
        </Style>
      </Ribbon.ContextualTabGroupStyle>
      <Ribbon.ItemContainerStyle>
        <Style TargetType="RibbonTab">
            <Setter Property="Visibility" Value="{Binding Visibility}"></Setter>
          </Style>
      </Ribbon.ItemContainerStyle>
      <Ribbon.TabHeaderTemplate><DataTemplate DataType="{x:Type ribbon:RibbonModelTab}">
        <TextBlock Background="Aquamarine"  FontSize="18"  Foreground="Green" Text="{Binding Header}"></TextBlock>
      </DataTemplate></Ribbon.TabHeaderTemplate>
      <Ribbon.ApplicationMenu>
        <RibbonApplicationMenu x:Name="_AppMenu" ItemsSource="{Binding AppMenu.Items}" />
      </Ribbon.ApplicationMenu>
    </Ribbon>
    <anal:Main1 VerticalAlignment="Stretch" Grid.Row="1" Grid.Column="0" x:Name="Main1" ViewModel="{kmWpf:Resolve anal:Main1Model,Throw=false}" />
  </Grid>
</RibbonWindow>